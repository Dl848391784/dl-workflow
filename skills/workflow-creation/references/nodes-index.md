# 编排节点子步骤索引（每节点几步/关键不对称/消费契约）

> workflow-creation skill 按需参考。本文件是 engine `Step.purpose` 的**第三通道手工副本**——
> purpose 实质改动（判据口径/分类清单/裁决点）后必须手工同步此处；phase-rules + 注入双通道
> 靠 GENERATED 渲染自动同步，本文件不会（2026-07-28 实例漏同步过一次）。
> 本文件只含**节点结构索引**（几步/每步干什么/关键不对称/消费契约锚点）；
> 拆步的第一性原理方法论在 `node-design.md` §3.8。


> **P3-1 读回分级（2026-08-13 用户裁决，v4-cost-latency-optimization-design §2 P3）**：
> 8 个读回步（u:1#7 / u:2-4#5 / plan:1#6 / plan:2#5 / plan:3#6 / plan:4#5，short=读回确认/读回装配）
> 已降**确认级**（`Step.tier="confirm"`，dl_flow_nodes 末尾单点补丁）——下方各节点摘要中这些步的
> 交互读回描述（弹卡片/逐问/裁决提问）**已退役**：现行为 = driver 机械展示（render-readback）+
> 装配（render-artifact，plan:1 为 design.md slug=工作流名）+ write_confirm_trace 静默通过落 trace，
> 无模型会话、无 prep 段；异议走 /dl state-reset 回上一步。交互步仅剩 5 个 decision 级：
> u:1-4#1 + plan:1#2（NEXT_PREP 合并段只对这些步生效）。

**子步骤编排（v2.7，§node-step-orchestration + §substep-gate-at-stop）**：某些子阶段（当前 understand:1/2/3）声明 `sub_steps`--有序子步骤序列（调 skill / 调工具，各有 purpose + record + gate）。**门控单位 = 子步骤**（不是子阶段级 rubric）；**skill 内部 Q/A 不门控**只 record。understand:1 = 7 子步骤（子1 逼问定义[结论二选一=载荷顶层「结论」键①/②开头，append-trace 机械校验存在性+前缀（v2.37 extra_payload_keys），judge 只判内容与出处质量；v2.51 原话形态枚举双侧钉死（_USER_QUOTE_FORMS_RULE：自由文本原话标「自由输入」/选中项标签标「（AskUserQuestion 选中）」属会话事实级自述合法、禁标「原话」/未自述如实标注——用户全程只点选项时按选中标注即合法，不逼打字；用户只点选项 = 逐问原则常态，「who 只认用户自述」与选项点击的接口此前未定义，judge 临场发明「原话全文引用」要件致三连 block，§3.5 #7+#4 复合）+ mech user_quote_channel（「原话」声称+AskUserQuestion 出处无通道标注拒、选中标原话=标注失真拒，att1-3 逐字 BLOCK/合法两态零 FP）；v2.52 词表扩「自述」（模型换词绕 v2.51 词表，逐字取新 att1）+锚定改「标签+引用」近端形态防元讨论 FP+规则文本撤「自述」类别名（§3.5 #23：v2.51 文本把选中叫「合法自述」与 judge 按「自述=等级声称」判矛盾，修文本撤词——「原话/自述」只许用于打字文本）+judge 抗已修还判双钉（run_judge 一致性①扩：判 block 引违规内容须是本轮产物原文短语、引不出不得作依据；mech_scope 钉句：写侧机械校验已过的形式要件勿重复判——att2 按判词范例改干净仍被照前轮判词描述 block 第二实例）；v2.64 痛点可观察性裁量点双侧钉死（_PAIN_OBSERVABILITY_RULE：可观察后果=问题不解决时的下游损害[须指明哪个下游环节因此做出什么不同动作]，修复诉求（要定位根因/修代码）与认知信任类（不合常理/不敢再用/报告失信）自称「可观察痛点」也不算，认知类同列=合法附带记录、仅纯认知痛点判 block + 模型侧退路=追问「这会改变什么动作/决策」或按②申报——混合后果（可观察+认知同列）处理规则此前未定义，judge 发明「每条列举后果都须可观察」要件致两连 block；重放两轮逮住初版过松：修复诉求被吞并为可观察后果、丰富载荷实质满足放过，逐字点名反例后 att1 BLOCK/att2 PASS/全认知规避 BLOCK 三向齐）；v2.65 解析器宽容化+报错带 repr+S15 放行 od/hexdump/xxd（手写载荷标头粘内容同行旧 regex 误拒、报错「从【purpose】开始」与文件实况矛盾致模型 xxd/python3-c 字节 hunt 死循环 8 报错；标头 regex 放行粘头 group(2) 注入首行+剥 BOM，junk 报错附 repr；--scaffold 生成正确格式是第一道但模型会绕过、解析器宽容是 defense-in-depth）；v2.66 堵旁路正治——fence S14 禁手写 Write .trace-payload-*.md（用户质询「宽容是软解」：模型手写粘头=手写格式=四桶违规，正治是堵入口非宽容输出；scaffold 生成格式、模型只 Edit 填待填，注入文案从「推荐」升必经路径；三层=指引→围栏→解析器宽容 defense-in-depth）] / 子2a 规划拆解[MECE 原子问题清单 + 逐原子定取证深度档（v2.xx[2026-08-13 取证重设计：值不值得取证判据（外部取证不改变结论方向→none）+ light 例删「量级合理性」+ tier 前置 MECE 后]；v2.40：载荷顶层 atomic_questions 键逐项 q/tier[none|light|full]/tier_reason——extra_payload_keys 泛化[spec=字符串注册名→数组逐项校验]，none 档理由须含仓内路径指针；默认档 light 拿不准兜底；judge 判分档合理性、重点抽查 none 档漏取证；v2.56 外部知识依赖操作测试双侧化（论证过程用到仓外公式/常识量级=含外部知识依赖不得 none——模型 Why2 引 M19 年化公式判量级却标 none 实证，枚举原只在 gate 侧=单侧钉死）；v2.50 +atomic_mece_alignment——aq 首字母标签与 MECE 声明「原子 X」集合对齐机械校验[att1 声明 3 原子交 5 条 D/E 未声明、同标签重复=一原子多条均当场拒；声明&lt;2 或 aq 无标签跳过交 judge，宁纵勿枉]，judge 不再碰计数——att2 判词搬 att1 旧计数失真失去），本步只规划不挖链（挖链归子2b）] / 子2b 因果链挖掘[根因因果链（主链每环须实际证据指针 file:line/数据值/日志原文/原话——「未实测/推断」标注不算出处、只允许保留理由（标「待子3取证」）；排除=断言假设为假须证据指针、推测式排除判 block（v2.49 排除/保留拆分——修判据自相矛盾：旧文本允许推断进排除分支、judge att3 却按「排除须证据」block，模型按写出来的规则做被没写出来的规则 block），链环全候选化=主链缺失判 block；「读出即事实」≠「读出后推出」——file:line 背书代码文本、量级是推断不算出处；挖不动的深层整体降格进竞争假设分支标「待子3取证」（降格=移出主链改写进假设分支——链环写「降格」声明仍占主链环位=未降格；拼接产物路径≠证据指针；行号跨度 ≥50 行不算精确指针，v2.49 三条反例双侧钉死），主链挖到实测层即终止；不要求根因已证明(证明归子4/5)；Read 是本步合法取证通道，Bash 被 engage 围栏拦是设计内≠禁取证；v2.37 链环含未实测/待实测/未验证/待验证/可能 append-trace 机械拒 mech_checks=causal_ring_no_untested；v2.49 词表扩面「需…验证类待办桥接/若…则假设形态/≥50 行行号跨度」+假设项豁免收窄（排除句含推断词形照拒，词形取自本轮 3 条真实被 block 载荷逐字，重放分隔度验证）；v2.50「待子3取证/降格」声明独占 Why 环且环内无 file:line 指针=占环位机械拒（环终止实测层+尾部降格去向声明合法——att1 逐字取词、att3 形态与自查项零 FP；同日第二 episode 三连 block 复盘）+「每环有 file:line」≠「每环是因果环」正反范例双侧钉死（正例=异常数值形成机制[_section_backtest.html:69 原文 _ann_pct=(_ann*100)]，反例=schema 路径/调用拓扑/数据契约描述有指针也不算[PROJECT.md:1200 产物路径]——att2/att3 操作化分歧，模型自查全过仍三连 block）；v2.55 全局否定断言「没有/缺失 X」（无显式契约/无 unit test/契约缺失）=「读出后推出」同族，合法出处=全域扫描零命中留痕否则降格，词形项级扫描机械拒（初版锚 WhyN 分段被 Why1=/Why4（根因层）= 形态空转，改项级——§3.5 锚定面条目） + 竞争假设，invoke causal-inference-root-cause] / 子4 双向取证[v2.38 外部层卸子代理：主会话 claim 可检验化→fetch-prompt --out 骨架落盘 per-workflow 目录（v2.42：路径 engine 钉死 .claude/workflows/&lt;name&gt;/fetch-prompt-skeleton.md——此前 engine 只出 stdout、落盘路径由模型自选，tail_volume 实例选了共享 evidence/ 通用文件名=无归属/下轮覆盖/残留误导；v2.43：「模型是否真用了 --out」下沉 append-trace 机械核验 fetch_skeleton_out[EXISTS+entered_at 新鲜度，§8.3 同范式]，不再靠文案；逐字命令模板：arXiv https+UA/GitHub 认证头/SE withbody 替页面/弃 WebFetch，2026-08-01 本机诊断版）只补 claim 区→每原子一个 Agent 并行→蒸馏报告收录=--ingest-agent（v2.60：脚本按 task-id 提取报告原文落载荷 qa 节，禁手工粘贴——粘贴=转录属四桶违规；append-trace 机械核验「蒸馏报告」项存在，judge 重放实证无报告项会被裁量放过故下沉）+内部仓库层主会话 codegraph 新鲜度前置；禁 tavily/WebSearch/WebFetch；禁训练记忆冒充外部证据；v2.39 执行序钉死「先派发后内查」（同日复盘实测：先内查 3min 再派发=空等 agent 10min，串行白烧 ~20% 步耗时）+fetch-prompt 纪律 8/9（摄取截断 head -c 6000 或落盘再读/轮次上限 ≤12 次 curl——Q4 取证 agent 20 轮 curl 上下文 29k→60k，遇 provider 空响应重试 26 次烧 1.19M input 占其 90%）+gate 裁决记录带 subagent_retry 统计（agents/empty_responses/burned_input_tokens，provider 稳定性台账）；v2.xx[2026-08-13 取证重设计：权威源注册表定点抓+术语翻译+泛搜兜底]；v2.40 按档执行（标称档=子2a atomic_questions：none 禁派发仅内查/light ≤2 层源 ≤4 curl 单向锚点/full 五层源双向现状；禁降档——纠偏归子2a gate；light→full 升档留痕[建议升档须补派 full agent 并原文收录理由]；fetch-prompt 预填 [tier=X] 标记+分档执行参数块，claim 区只留本原子行——台账按标记归属；fetch_report_recorded 改 tier-aware 数报告项[none 豁免]；台账扩 per_agent tiers+curl_calls，纯 light>4 curl 记 light_tier_violations，混合标记保守不计）] / 子5 质检裁决[三关质检(针对性/独立性/可追溯)+条件触发红队(独立上下文——「只给证据不给结论」指红队**输入**不含子5 初步结论，redteam-prompt 机械保证；红队**输出**必含四态 verdict 属合规；u1-sub5-cost[2026-08-17]：红队改 **driver 预派发**[Step.pre_dispatch="redteam"——子4 gate 过后证据冻结即具备输入，driver 派子5 段前起 `claude -p --tools Read` 后台 worker 与本步段并行，实测红队 158-235s vs 主会话有效并行 ≤1min；收录主路径=--ingest-redteam[阻塞等报告就绪落载荷，worker.json/redteam_report.md 协调]，无预派发回退会话内 --ingest-agent；mech 闭环=worker.json 在位无收录项当场拒]，旧路径=--ingest-agent（v2.60 同上，禁手工粘贴），提及/转述不算记录；v2.44 收录形式要件机械化 mech_checks=redteam_report_recorded——收录项标题须含「红队」「原文收录」，trace 含 task-id 或预派发 worker 在位（=已派发）而无收录项 = 红队未归提前提交当场拒（v2.37 占位符扫描被「未归/仍在跑中」措辞绕开的实证收口，词表打地鼠升级为结构信号；未派发交 judge 判真值）+四态 verdict(证实/证伪/部分成立/证据不足)；u1-sub5-cost 修1=_TASK_ID_RE 排 float repr 误报[候选须含 a-f 字母+前邻非 hex/`.`——证据 float 小数位被当 task-id 假阳性 4 次拒]、修2=redteam_three_piece 置信度词形放宽 `置信度|置信\s*\d`[真实被 block 载荷「置信 95%」逐字]+redteam-prompt 模板钉逐字标签[双侧])] / 子6 归一化陈述[原子(单句≤1独立痛点)+去上下文(主语+动词+约束自包含)+携带 verdict 边界/置信度；陈述集与子5 verdict 逐项一致——证伪项不进陈述集、部分成立项不超已证实边界；v2.36 迁 statements 格式(type_label=verdict/fields.confidence)，方案名词机械扫描生效——v2.33 迁移漏网补齐] / 子7 带证据读回确认[呈现材料=render-readback 机械装配（v2.61 禁手抄）：陈述+verdict+证据指针+置信度；证据不足显式暴露由用户裁决；多问题选定本实例处理项，其余落 evidence 供后续 dl 实例；v2.45 用户裁决必入 trace 机械校验 user_decision_recorded——8 个读回步（u:1-4 末步+plan 各末步）全 gate=None 无 judge，裁决只在对话里=/clear 交接即丢，标题带「裁决/读回」qa 项+≥50 字内容是唯一防线（_USER_DECISION_RECORD_RULE 单源双侧披露；真实 u:1 子7/u:2 子5 载荷重放 PASS，空记录/缺项 BLOCK）]），子1/2/3/4/5/6 gate 跑 judge 校验 evidence 里的 skill-trace，子7 gate=None（trace 存在即过）。v2.6（2026-07-25）：4→5 子步骤，插入子2 拆解深挖——复合问题 MECE 切分不丢弃 + 纵向挖根因防叙事式深挖。v2.7（2026-07-26，`designs/step3-verify-redesign-design.md`）：5→6 子步骤，旧子3 单步「验真」拆为子3 双向取证 + 子4 质检裁决——第一性原理消 F1 主张不可检验/F2 确认偏误/F5 证据不可追溯/F7 单视角四类失效；拆步按失效模式族（取证过程 vs 判断质量），一步内可编排多工具（ref 是声明式标签，engine 不限数量）。v2.8（2026-07-26，`designs/step5-step6-statement-readback-redesign-design.md`）：子4 加④按 verdict 处置问题集、子5 一句话陈述重定义为归一化陈述（claim normalization，消「裁决不传导」缺口）、子6 读回确认升级为带证据读回（消「无依据确认」缺口）。**plan-first 拆步（2026-08-14，`designs/understand1-sub2-plan-first-split-design.md`）**：6→7 子步骤，子2 拆解深挖拆为子2a 规划拆解（MECE 切分+tier 定档，本步只规划不挖链）+ 子2b 因果链挖掘（根因因果链+竞争假设），旧子3-6 顺延为子4-7（双向取证/质检裁决/归一化陈述/读回确认）。——ProblemContext 终态三属性：内容正确(子1-5)/形式可移植(子6)/用户认可(子7)。**推进走 Stop hook**：模型完成一步 **写载荷=分节标记文本（v2.58 .md 零转义【purpose】【q】【a】，模型零接触 JSON；推荐 --scaffold 起骨架填「待填」，v2.57）+ Bash `append-trace --from-file` 落 evidence（v2.14 起；格式/路径/结构字段归脚本；旧 q/a 平行数组 v2.35 硬拒）-> 输 `### STEP_DONE: <n>` -> end_turn**；Stop hook 比对 evidence 当前子步骤最新 trace 行 hash 与 state.last_judged_trace 游标--有变化才判（区分「完成」vs「中途暂停等用户」，也防覆盖写漏判）：pass 推进 + **非末步自动续轮**（2026-07-25 决议：pass 也返 additionalContext 指令模型当轮开做下一子步骤，免用户每步发「继续」；2026-07-27 起**跨子阶段同样自动续轮**——无门栏的边界不是检查点，门栏才是：末步 pass 且下一子阶段有编排 -> 当轮开做其子1；仅门栏节点末步扣留停轮）/ block 当轮 `_block_continue` 返工（返工须 append 新 trace 行）/ 连续 block 3 次升级为 AskUserQuestion 用户裁决。有 sub_steps 节点不用 SUB_DONE（互斥）。**回退重测**：`/dl state-reset <n|phase:minor[:step]>`（engine reset_state，v2.22 起替代 step-reset，`designs/state-reset-command-design.md`）——含目标 step 作废：删该 step 及之后的 evidence（trace 按 minor_stage、gate 按 phase+sub 归属；坏行/归属不明行保留）+ 删目标 phase 及之后的阶段产物 + 清 last_judged_trace 游标/node_attempts/held_for_gate + history 截断；`<n>` 本节点内回退，`phase:minor[:step]` 跨节点整体回滚（minor=子阶段序号或 minor_key；节点级 gate 裁决行在目标节点上也删——回退到中段旧「已过」已失效）。v2.9（2026-07-26，`designs/subphase-hold-gate-design.md`）：**子阶段门栏 hold_for_gate**（2026-07-28 用户决议收窄为**唯一处 = plan:4**——围栏只设在 plan 完成，understand:2/3/4、plan:1/2/3 门栏全部撤除）——末子步骤过门控后**无条件扣留不推进**（state.held_for_gate；不读 state.gate 防中途 /dl gate 预放行泄漏穿栏），唯一出口 `/dl gate`（dl-cmd 检测 held 路由 engine `subgate-pass`：写 manual-subgate-pass 裁决留痕 + 清标记 + 推进）；`/dl step-pass` 末步同被扣（步的放行 ≠ 子阶段的放行）；`step-reset` 清标记。
> **understand:1 子2b 取证入口机械提取（v2.44，2026-08-16，`designs/u1-sub2b-mechanical-symbol-extraction-design.md`）**：回炉 v2.43 模型手写 symbols/files（实测子2a 16→119 轮爆炸，净亏 2.3×）。改为机械导出：`dl codebase query --string` 命中时自动落账 `string-files` 摘要（只记 files 不记全文）到 discoveries.jsonl；`ensure_node_rules` 对 understand:1 注入两块——①子2a atomic_questions 全文（「查什么」）②台账 string-files 条目（「去哪查」）。模型零手写取证入口。

**understand:2 = 5 子步骤（v2.15，2026-07-26，`designs/goals-and-value-substeps-design.md`）**：子1 目标引出[KAOS WHY/HOW 问；双结论制——「目标不成立」合法防逼编造价值；v2.48 价值/结论项推断词形（隐含/意味着/可能，排除「不可能」）append-trace 机械拒 mech_checks=value_no_unsourced_inference——形式要件双侧披露后模型仍写入「长期使用意味着…」且自声明「无推断补全」（8/2 att1 实证：自声明不可信，词形下沉机械层，同 v2.37 u:1 子2 先例）；「推测」另列合法豁免；purpose/gate 单源常量补「读出即事实 vs 推出」范例；重放 att1 BLOCK/att2 PASS/demo 零 FP] / 子2 对齐质检[双向追溯矩阵(防镀金+防漏)+solutioneering 剥离(WHY 问剥到 outcome)+目标间冲突检测；冲突留子5 用户裁决] / 子3 价值论证与分层提案[受益者+价值链+量化基线(不可量化显式标注合法)+must/nice 提案附理由；分层只提案不拍板] / 子4 归一化陈述[原子+去上下文+携带分层与 verdict 边界+solution-free 复核] / 子5 读回确认[用户裁决 must/nice——唯一规范裁决点；gate=None]。**关键不对称**（步数 5≠6 的第一性原理）：问题是事实性命题（需外部取证+质检裁决），目标/价值是规范性命题（外部证据无权证伪「我想要什么」，真值源只有用户）——故无取证/裁决双步。**无 hold_for_gate**（2026-07-28 用户决议：围栏只设在 plan 完成）——末步过门控自动续轮进 understand:3。**v2.15 机制配套**：trace 匹配层加 minor_stage 过滤（_iter_trace_segments/sub_step_has_trace/latest_trace_sha1/read_evidence_for_step/corrupt_trace_after_latest/evidence_mentions_sub_step，None=不过滤向后兼容）——多编排节点共用 evidence 且 sub_step 都从 1 起，不过滤会跨节点串号（ProblemContext 子1 trace 被 GoalsAndValue 门控误读）。
**understand:3 = 5 子步骤（v2.16，2026-07-27，`designs/scope-and-constraints-substeps-design.md`）**：子1 障碍分析引出[KAOS 否定提问「什么会使它失败」逐 must 目标；约束分类按编程域（代码库结构/项目硬规则一等约束源/数据契约/环境工具链/外部依赖/时间资源）；双结论制——「无实质约束」合法但须否定提问留痕] / 子2 约束验证标注[三态：已验证(本地单层源 Bash/codegraph/Read+工具留痕；硬规则约束验证源=Read 规范文档原文引用，codegraph 新鲜度前置)/假设(置信度+影响显式标注)/证伪；fence_allow=Bash；只标注不裁决] / 子3 范围界定[in/out 双侧清单落改动面——文件/模块/symbol 级，改动面证据=子2 已验证留痕逐字引用为默认形态（标注来源，零重查；新查询=逐项例外单点补——u3-sub3-cost，「缺口才新跑」宽松版被弱模型读成主动找缺口验证致探索爆破）+双向追溯矩阵+约束回写；只提案不拍板；Step 级 segment_strip_project_context 置位（消费型步首例——规则内容经子2 trace 在场，B1 节点级决议不下放）] / 子4 归一化陈述[原子+去上下文+类型标签(已验证/假设+置信度/in/out)+solution-free 复核；纯消费装配步（text 取自子3 outcome 标签禁二次创作）——u3-sub4-cost 置位 Step 级 segment_strip_project_context+pack_self_contained（u:3#3 同型第二例），purpose 钉死「格式真源=scaffold 骨架+append-trace 报错文案，禁读引擎/测试源码反推校验实现」（基线格式猎捕 9 次调用实证）] / 子5 读回确认[两个规范裁决点：范围拍板+假设接受(风险承担)；gate=None]。**混合命题不对称**（与 1/2 都不同的第一性原理）：约束=事实性但只需本地单层源（压缩 ProblemContext 取证+质检双步为子2 一步），范围=规范性（拍板归用户），假设=中间态（接受=风险承担归用户）。**无 hold_for_gate**（2026-07-28 用户决议：围栏只设在 plan 完成）——末步过门控自动续轮进 understand:4。同版本配套：节点树自 dl_flow_engine.py 抽出为 `dl_flow_nodes.py`（声明式数据与机制逻辑分离，engine re-export 访问面不变）。编程域修订同日（编程专用工作流定位，见设计文档头部修订决议）。
**understand:4 = 5 子步骤（v2.17，2026-07-27，`designs/success-criteria-substeps-design.md`）**：子1 成功标准引出[INCOSE 验收视角提问「怎么知道它达成了」逐 must 目标+双向追溯+solutioneering 剥离；双结论制编程域收紧——「只能定性验收」=稀有合法结论，须说明「为何不可执行验证」（代码行为几乎总是可执行验证，合法剩余≈UX/可读性/架构审美）；u4-sub1-cost 置位 Step 级 strip+pack_self_contained（交互步置位首例——材料=GAV/SC step4 statements 全文在包+prep 载荷 sources，复用钉死=零新取证/零子代理/禁 evidence 全量翻找，file:line 手段取证归子3）] / 子2 可检验化[Volere fit criterion：模糊词改写+度量指标+基线(Bash 实测)+阈值只提案；可执行验收优先（failing test/脚本断言/命令+退出码，specification by example 接 TDD）；不可检验=合法退回信号，禁硬编假指标；fence_allow=Bash] / 子3 验收方式设计与可行性验证[INCOSE 四法编程映射(test→pytest/验证脚本，analysis→数据 log 对比，inspection→review checklist，demonstration→跑起来看行为)+可行性三态(存在附出处/待建标注=测试框架 fixture 脚本缺失进 plan/剔除)+时机标注(triggered/continuous，事后验证标风险)+证据形式锚定 review:0；fence_allow=Bash] / 子4 归一化陈述[原子+去上下文+携带六字段验收包(指标/基线/阈值提案/方法/时机/证据形式)] / 子5 读回确认与 understand.md 装配[两个规范裁决点：阈值拍板+验收方式认可(含「待建手段」是否接受为任务项)；裁决后装配 understand.md=render-artifact（v2.59：脚本从归一化/裁决/未选定 trace 机械装配落主仓，禁手写产物）；gate=None]。**关键不对称（第四种）**：混合命题，轴心=规范性目标的可检验化转换；**消费契约锚点**——验收包六字段倒推自 review:0 rubric「判定 solved/partial/not 附 file:line 证据」。**无 hold_for_gate**（2026-07-28 用户决议：围栏只设在 plan 完成——understand 移出 GATED_AFTER）：understand.md 子5 内装配（artifact_on_release=False，同 plan:2/3/4 产物节模式），末步过门控自动续轮进 plan:1（跨阶段自动续轮，无 PHASE_DONE: understand 通道）。机制遗留：`phase_done_channel_open` + 注入第三态现仅服务 plan:4。编程域修订同日（编程专用工作流定位，见设计文档头部修订决议）。
**「方案名词」裁量点钉死（v2.23，2026-07-30，tail_volume understand:2 子4 block 复盘）**：understand:2 子2/子4、understand:3 子4、understand:4 子1/子4 共 5 步的 purpose/selfcheck 披露操作化定义（单源常量 `_SOLUTION_FREE_SUBJECT_RULE`）——主语只许 outcome-level 概念（用户可见的状态/数字/信号），模板文件名/类名/CSS 类/函数名/字段名/管线名/组件名等实现侧名词入主语=违规，file:line 与实现指针留边界字段与 evidence 不进主语；gate 判词不动（主语词性检查=形式要件，披露不 Goodhart，§3.5 #2/#4）。复盘定性：首判模型把 solution-free 核查落在谓语、judge 裁量落在主语——同一判据两种读法（判据留白=方差），非注意力失败。
**裁量点钉死双侧化 + 范围步动词指向判定（v2.24，2026-07-30，tail_volume understand:3 子4 五连 block 复盘）**：v2.23 只钉模型侧的缺口暴露——judge 输入面仍只有黑盒判词，五轮 block 解释逐轮漂移（类名入主语→实现指针段含行号→动词词形），同一 rubric 五种读法。修订：①`_SOLUTION_FREE_SUBJECT_RULE` 扩进 5 同构族 gate（judge 侧，对齐 `_DS_STEP1_FORM_REQUIREMENTS` 双侧单源先例；实现指针/file:line 在边界字段与 evidence = 合法钉死）；②新增 `_SCOPE_VERB_RULE` 只挂 understand:3 子4——动词**按指向判不按词形判**：指向代码实现动作（改哪个文件/函数/模板/CSS）=违规，指向用户可见状态/范围赋予=合法，in/out 范围陈述的构成性谓语「允许/禁止改动」合法（judge #5 把它判违规=把范围命题判成不可表述，用户强制放行收场）；目标/标准陈述（GoalsAndValue/SuccessCriteria）动词判据不动——「做一个X」在目标语境仍是真信号。测试 `TestSolutionFreeRuleInGates` 钉死双侧引用不回归。
**statements 结构化 record + 机械预检下沉（v2.27，2026-07-30，同一审计的弱模型优先系列）**：q/a 问答模具与清单型产出语义错配（3 次长度不齐全在归一化步）+ 词形判据不该花 judge 调用——Step 加 `record_format` 声明，u:2 子4/u:3 子4/u:4 子4（归一化+solution-free 族）改 `statements` 载荷（`{"text","type_label","boundary"}` 逐项，实现侧名词/file:line 只能进 boundary）；append-trace 机械预检①三字段非空 ②text 方案名词扫描（codegraph class/function/method + git ls-files 文件名真值，文档扩展名除外——规范引用合法；命中即拒并点名指路挪 boundary，省 judge 调用+消裁量方差）③源步 ID 传导覆盖核对（step.input 溯源，in[..]/out[..]/Cx.x 缺传即拒——u:3 子4 judge #1 的活）。u:1 子5 无 solution-free 判据留 qa。注入示例按步 record_format 渲染（`payload_format_hint` 单源）；evidence_show 支持 statements 展示。测试 `TestStatementsRecordFormat` 9 例。
**plan 含 2 子阶段（v2.18，2026-07-27，`designs/design-solution-substeps-design.md`）**：plan 拆为 plan:1 设计解决方案（编排）+ plan:2 拆解任务与阶段（原 plan:0 重编号；v2.19 起也有编排，见下段）。**plan:1 = 6 子步骤**：子1 代码现状勘察[现状地图四要素(模块与现有实现/可复用点/调用方影响面/数据契约)+codegraph 新鲜度前置；每条附 codegraph/file:line 出处；fence_allow=Bash] / 子2 方案发散[≥3 个代码级候选(改哪个模块/函数/新增什么文件)+架构维度实质差异+禁评估禁排序；双结论制——「设计空间唯一」合法但须逐维度论证；用户既有想法平权入列] / 子3 可行性验证与假设标注[五项核验(存在性/重复造轮子/影响面 impact/硬规则 H1-H13/可测试性)+三态(可行附出处/假设+置信度×影响/证伪剔除)；fence_allow=Bash] / 子4 评估收敛与选型提案[Pugh 矩阵(判据=验收包承接度+改动面+影响面+复用度+可测试性)+双向追溯+条件红队；只提案不拍板；fence_allow=Agent] / 子5 归一化陈述[原子+去上下文+携带代码设计包八字段(改动清单/接口签名/数据契约/callers 清单/被否方案+理由/假设清单/验收包映射/H9 执行单元)] / 子6 读回确认与产物装配[三个规范裁决点：选型拍板+权重认可+假设接受；拍板后装配 design.md=render-artifact --slug（v2.62：slug 命名留模型，装配归脚本；已存在拒覆盖 --force 放行）；gate=None]。**关键不对称（第五种）**：创造性生成×代码接地**双轴心**——首个创造性生成节点（对象不存在须发散，防 design fixation——自我固化是最强固化源）且解必须锚定本仓代码现实（须勘察，防凭空 API/重复造轮子）；**消费契约锚点**——设计包八字段倒推自 plan:2/execute:0/review:0 三个下游 rubric；**编程工作流定位**（用户 2026-07-27 决议）：候选=代码级设计，禁理论方案空谈。**无 hold_for_gate**（2026-07-28 用户决议：围栏只设在 plan 完成）——末步过门控自动续轮进 plan:2。**v2.32（2026-07-31，tail_volume plan:1 子5 三连 block 审计）**：子5「复合句」裁量点钉死——`_ATOMIC_ITEM_RULE` 单源常量：原子性按独立性判不按词形判（字段键值枚举/括号携带=结构化携带不算复合；复合=一项合并 ≥2 个可独立拍板项），purpose/selfcheck/gate 双侧引用。**v2.33**：子5 迁 `record_format="statements"` + `statement_fields` 八键（change_list/interface_sig/data_contract/callers/rejected/assumptions/acceptance_map/h9_units）——字段齐备由 append-trace 逐键机械校验，text 只留单句决策，实现指针进 fields/boundary（`designs/plan-normalization-statements-migration-design.md`）。
**plan:2 = 5 子步骤（v2.19，2026-07-28，`designs/task-breakdown-substeps-design.md`；label「生成执行计划」→「拆解任务与阶段」）**：子1 设计包清点与追溯基线[三清单(原子改动要素带 ID/验收包/假设)+每条附出处+要素原文引用进 trace(design.md judge 读不到)；只提取不创作；新增候选显式列待子5 裁决；fence_allow=Bash] / 子2 任务切分与依赖排序[writing-plans 粒度真源：单元=自带测试周期最小单位，纵向切片优先(横向须辩护)，H9 预算+承接要素 ID+依赖出处；codegraph DAG 拓扑排序+TDD 序内嵌；阶段=可整体验证/提交/回滚单元组；双结论制——「单阶段不可拆」合法但须论证；只提案不拍板；fence_allow=Bash] / 子3 锚点核验与假设标注[四类核验(文件 symbol 存在/测试接缝/验证命令可运行/No Placeholders 检出)+三态(已验证附出处/假设+置信度×影响/证伪回子2)；fence_allow=Bash] / 子4 归一化执行步骤[原子+去上下文(零上下文执行者可做)+携带执行包五字段(改动点/前置接口 Consumes+Produces/验证方法=可执行优先 spec by example/验收包映射/追溯锚)+假设传导] / 子5 读回确认与 plan.md 装配[两个规范裁决点：阶段/粒度拍板+假设接受；拍板后装配 plan.md=render-artifact（v2.59 禁手写产物）；gate=None]。**关键不对称（第六种）**：保真转换×执行接地——与 plan:1 镜像：输入对象**已存在且已拍板**（创造性已上游消费，**无发散步**；设计包字段⑧已预选切片），主敌是转换失真（semantic drift，防=要素 ID 基线+追溯锚 goal anchoring）与执行者视角虚构（零上下文 executor 把编造锚点当事实，防=锚点核验步）；**消费契约锚点**——执行包五字段倒推自 execute:0/executing-plans/review:0。**节点级 gate_rubric=None**（understand:4 先例：语义下沉逐步 gate，大闸门只跑机械门）+ **gate_mech=ARTIFACT_CONTAINS（「执行步骤」节，v2.41 由 EXISTS 升——phase-rules 装配行同步补节名，此前连节名都没给）**。**无 hold_for_gate**（2026-07-28 用户决议：围栏只设在 plan 完成）——末步过门控自动续轮进 plan:3。**v2.32**：子4 同挂 `_ATOMIC_ITEM_RULE`——每项=1 个可独立验证/提交的交付物，TDD 微循环（失败测试→最小实现→验证→提交）=交付物内含流程不算复合（tail_volume plan:2 子4 三连 block + 用户强制放行实证）。**v2.33**：子4 同迁 statements+statement_fields 五键（change_point/interface/verify/acceptance_map/trace_anchor）。**v2.20 变更（plan:3 加入）**：本节点不再是 plan 末子阶段，**advance 由 "phase" 改 "sub"**——hold 语义从 understand:4 同构转为 understand:2/3 同构（放行后自动续轮进 plan:3 子1，不再有 PHASE_DONE 通道；`phase_done_channel_open` 对 advance="sub" 恒 False，`artifact_on_release` 字段对 sub 节点不被读取，故不再显式声明）。
**plan:3 = 6 子步骤（v2.20，2026-07-28，`designs/capability-tool-selection-substeps-design.md`）**：子1 需求清点与追溯基线[逐任务操作类型清单(代码改动=H15 信号/测试/长 pipeline/检索/数据读取/子代理/装配)+任务 ID 出处+plan.md 原文引用进 trace；只提取不创作；fence_allow=Bash] / 子2 能力盘点与强制路由核对[三通道注册表(skill 列表+磁盘目录/工具·CLI·MCP/强制路由核对=CLAUDE.md §2 触发词+H15+superpowers 触发)；能力名**逐字引用注册表出处**禁凭训练记忆；双结论制——「内置工具足够、零 skill」合法但须逐任务说明；fence_allow=Bash] / 子3 匹配选型提案[四判据：覆盖(引用 trigger 原文)/最小集(无绑定=不加载，tool overload 防线)/成本相称(重型手段辩护)/强制优先；双向追溯矩阵+条件红队；只提案不拍板；fence_allow=Agent] / 子4 可用性核验与假设标注[四类核验(skill 存在/CLI 可用/MCP 连接/环境前提)+三态(已验证附出处/假设+置信度×影响/证伪回子3 换绑)；fence_allow=Bash] / 子5 归一化能力包[原子+去上下文+携带能力包五字段(必先 skill+触发依据/工具清单+可用性状态/强制门禁对齐项/子代理策略/显式不加载清单)+假设传导] / 子6 读回确认与 plan.md 装配[两个规范裁决点：映射拍板+假设接受；拍板后装配 plan.md「能力与工具」节=render-artifact（v2.59 禁手写产物）；gate=None]。**关键不对称（第七种）**：有限枚举×配置接地——能力空间=**可枚举注册表非生成空间**（无发散步，发散=逼编造伪候选），主敌是映射层失效：幽灵能力（ghost invocation，f̂∉F）/能力错配（distractor tool）/tool overload（实测准确率 95%→71%，防=最小集+显式不加载清单）/漏配强制项；**消费契约锚点**——能力包五字段倒推自 execute:0/using-superpowers「必先 invoke skill」/H15+执行映射/Agent 成本决策/overload 防线五方。**gate_rubric=None + gate_mech=ARTIFACT_CONTAINS（「能力与工具」节，v2.41 升——v2.31 实现机械门、2026-08-02 节标题单源化后由 EXISTS 升 CONTAINS）**。**无 hold_for_gate**（2026-07-28 用户决议：围栏只设在 plan 完成）——末步过门控自动续轮进 plan:4。**v2.33**：子5 同迁 statements+statement_fields 五键（skill_first/tools/enforce_align/subagent_policy/no_load；`_ID_RE` 同步扩面认 RC-A/T1/SC4.1/#1a/U1——旧模式 plan 域 ID 传导静默空转）。**v2.21 变更（plan:4 加入）**：本节点不再是 plan 末子阶段，**advance 由 "phase" 改 "sub"**——hold 语义从 understand:4 同构转为 understand:2/3 同构（放行后自动续轮进 plan:4 子1，不再有 PHASE_DONE 通道；`phase_done_channel_open` 对 advance="sub" 恒 False，`artifact_on_release` 不再显式声明），与 plan:2(v2.20) 同一变更路径。
**plan:4 = 5 子步骤（v2.21，2026-07-28，`designs/execution-plan-checkpoints-substeps-design.md`）**：子1 四源清点与追溯基线[五类清单(任务 DAG+阶段边界/能力绑定/验收包[triggered 项=检查点候选]/假设汇总/不可逆操作候选)+四源原文引用进 trace(design.md/plan.md/understand.md judge 都读不到)；只提取不创作；fence_allow=Bash] / 子2 调度与检查点方案提案[双对象：①调度四件——并行分组(DAG 拓扑分层)/文件互斥面(改动点计算，组内交集须空)/worker 任务包映射/subagent 返回契约(证据形式清单)；②检查点三属性——零判断词判据(命令+退出码，禁「确认/检查/合理」)/失败路由三选一预定义(禁「视情况」)/类型(不可逆操作前强制用户暂停)；goal anchoring 重述句逐检查点；密度按可逆性×爆炸半径提案；双结论制——「零用户检查点」合法但须复利数学论证；只提案不拍板；fence_allow=Agent] / 子3 锚点核验与假设标注[四类核验(判据 dry-run/互斥面交集实算[命令+输出进 trace]/锚点存在/验证手段有绑定)+三态；fence_allow=Bash] / 子4 归一化执行计划包[原子+去上下文+携带十字段(调度四：并行分组/互斥面/worker 映射/返回契约；检查点六：位置锚/通过判据/失败路由/类型/验收包映射/goal anchoring 重述句)+假设传导] / 子5 读回确认与 plan.md 装配[**三个**规范裁决点：密度与类型拍板(风险承担)+假设接受+**plan.md 冻结策略**(默认小偏离留痕/大改 /dl back，禁 execute 内改合同)；拍板后装配 plan.md「执行计划与检查点」节=render-artifact（v2.59 禁手写产物）；gate=None]。**关键不对称（第八种）**：时序控制×风险配平——首个**运行时控制结构设计节点**（plan:1/2/3 产物全是静态内容，本节点设计控制流：何时停/验什么/失败后去哪/谁可并行）+ 首个**四源聚合节点**；execute 愿景（2026-07-28 用户决议：executor 不再自行分析+多 subagent 并行，orchestrator-worker）钉死消费契约——判据零判断词/失败路由预定义/并行分组 plan 期定死全是硬约束；实证基座：Lusser's Law(85%⁸=27%)/self-conditioning/17.2× vs 4.4×(中心化编排压制错误放大)/Inspector 96.4%/arXiv 2605.30777 fabricated success reports。**消费契约锚点**——十字段倒推自 execute:0 orchestrator 愿景/execute:0 rubric 逐条核/understand:4 时机字段/review:0 证据需求/用户密度裁决五方。**gate_rubric=None + gate_mech=ARTIFACT_CONTAINS**（v2.31 落地「执行计划与检查点」节兑现 §5 #7 预先裁决；v2.41 扩为全三节——装配终点+唯一门栏查跨节点删改）。**hold_for_gate=True（全工作流唯一门栏，2026-07-28 用户决议：围栏只设在 plan 完成）**——末步扣留等 /dl gate（放行 ≠ 推进），放行后 PHASE_DONE: plan 撞 plan->execute 大闸门（第二次 /dl gate）；artifact_on_release=False。**v2.119**：子4 同迁 statements+statement_fields 十键（parallel_group/mutex_surface/worker_map/return_contract/cp_position/cp_criterion/cp_failure_route/cp_type/cp_acceptance_map/cp_goal_anchor）——v2.33 迁移漏网第九处补齐：qa 残留致 render-artifact 结构性跳节（只读 statements）、门栏 ARTIFACT_CONTAINS 不可通过（2026-08-06 tail_volume live 全轮首达 plan:4#5 实爆，designs/plan4-sub4-statements-migration-design.md）。**配套**：phase-rules execute 节加「首步=读 plan.md（三节）；按检查点停验，偏离留痕理由，禁执行期改 plan.md」硬指令（消费路径闭合，非 execute 编排化）。**plan.md 规范位置=主仓 `.claude/plans/<name>.md`**（2026-07-28 用户决议：与 evidence 同级——worktree 归档删除时分支上产物一起丢，主仓 .claude/ 才存活；S11 加路径规则限 plan 阶段写 + 注入「产物路径」行给绝对路径）。

**读回步通用——逐问原则（2026-07-30，tail_volume understand:4 子5 cache 审计）**：8 个读回确认/读回装配步（understand:1-4 末步 + plan:1-4 末步）的 purpose 统一挂载 `_INTERACTIVE_CHUNKING_RULE`——提问按预计用户思考时长分组：快答项（认同/接受/选定/无改）合并一轮先问；预计 >4 分钟思考的硬核裁决（拍板/圈定/冻结类规范裁决）单列一轮后问，禁捆绑（捆绑则由最难项决定总时长，>5min 未作答击穿 prompt cache，下轮全量重读——实测 4 问捆绑 370s，255.7k token 全价重读，占该窗口非缓存 input 一半）；读回材料在首轮提问前以文本完整呈现。设计新交互读回步时同样挂载该常量，不自写变体。

**产物节结构单源化 + CONTAINS 扩面（v2.41，2026-08-02，`designs/artifact-handoff-hardening-design.md`）**：`ARTIFACT_SECTIONS`（dl_flow_nodes）= 各产物「该带什么节」唯一真源——understand.md 四节（真实问题重述/目标价值/范围约束/成功标准验收包）/ plan.md 三节（执行步骤[plan:2 装]/能力与工具[plan:3 装]/执行计划与检查点[plan:4 装]）/ review.md（结论+证据）/ evolution.md（经验+落地，新增结构）。三通道同源：①engine CONTAINS 门（understand:4/plan:2/plan:3/review:0/evolution:0 由 EXISTS 升，plan:4 扩全三节）；②phase-rules 装配行 `{{artifact_sections:<basename>[#<idx>]}}` token（render_phase_rules 第二阶段替换，fail loud）；③注入 PHASE_RULES artifact 描述动态构建（SECTIONS_TEXT）。purpose/selfcheck/ref 内节名用 `_S_*`/`SECTIONS_TEXT` 常量插值**禁字面量**；同步测试 TestArtifactSectionsSync（engine 5 例 + 注入 2 例）钉四通道不漂移。**P3 回查通道**：execute/review/evolution 产物信息不足 → `evidence_show.py <name> <主仓根>` 回查证据链 → 仍不足 AskUserQuestion，禁凭训练记忆补全。**用户决议**：execute 台账本轮不做先观察（**自述式台账永久否决**——fabricated success reports 污染源，只接受生成式）；review/evolution 最小两节。**注意在飞实例**：已有产物缺节会在下次末步重判撞新门（block 可修复：补节+新 trace）。

