# u:3 子3（范围界定）gate framing 反转设计（v2.90）

> 2026-08-04。§3.5 #28 泛化例、#30 playbook 例（承接 u:1#1-#5 / u:2#1/#2/#4、并行 u:2#3）：
> 把「默认-PASS framing + 方框化真值判据 + 每条近端双侧钉死」应用到 understand:3#3（范围界定）。
> 坐标说明：用户口径「understand:3#3」= ScopeAndConstraints 子步骤3（范围界定步）。
> 版本号取 v2.90 = max(已入库 v2.88, 工作区并行 u:2#3 自称 v2.89) + 1（collab #6）。

## 0. 本节点与前例的结构差异（决定判面怎么切）

| 维度 | u:2#1/#2/#4 | **u:3#3** |
|---|---|---|
| record_format | qa / statements（#4） | **qa**（mech 下沉可行，⑰——本轮未触发，纯 gate 反转达标） |
| mech_checks | 有/无混合 | **无**（⑯：无可声明面，gate 不含「已机械校验」段） |
| artifact 组成 | 单条 / 子1+子2 / 子3+子4 拼合 | **子1+子2+子3 三行拼合**——子1 约束候选+子2 三态处置是子3 约束回写/范围上界的对照基准 |
| 真实载荷 | demo evidence 有真实 trace | **无真实 trace**（demo 未走到 u:3；tail_volume 被 state-reset 清档）——子1/子2/子3 三行按 demo GoalsAndValue 真实目标集（G1 用户裁 nice）合成，实现指针取真实路径（`summary/generate_factor_summary_report.py` 等） |
| 命题性质 | 事实/结构/归一化 | **规范提案**（in/out 范围清单+双向矩阵+约束回写）——拍板归子5 用户，judge 判形式完备+四类质量违规 |
| vio 类型（⑫） | 形式+语义混合 | **形式 2**（out 空清单/汇总无矩阵）**+语义 3**（矩阵放水/outcome 空泛/拍板）——语义主导，穷举违规形态为独立方框 |
| gate 长度 | 291–2397 | **306**（短 gate thrash **第七实证**：基线 clean 0/6） |
| 特殊判据陷阱 | — | **矩阵放水判据与 u:2#2 同名不同物**（问题-目标 vs 目标-范围）——措辞可移植、判材不同；**clean forward 行「G1→in[1]+in[2] 全覆盖」是判据五的边界形态**（点名标识无关系类别），诱发判官发明「标识也须给关系类别」要件 |

## 1. 基线数据（MiniMax-M3，n=6，现 gate 306 字从严版）

`RESULT_JSON: {"clean": "0/6", "vio1_out空清单": "6/6", "vio2_矩阵放水": "5/6", "vio3_outcome空泛": "6/6", "vio4_替用户拍板": "6/6", "vio5_汇总无矩阵": "6/6"}`

**clean 误判逐字聚类**（每类=一个合法正例钉死位）：

- **聚类 A（3/6）outcome 标签空泛过判**：把有信息标签（「数据截至日期与 14/72(19.44%) 规模可见」「报告展示页面保持现状」）读成「页面相关」式。三变体发明要件：未落到 outcome 层（要求指向用户价值/验收口径）/未标注 outcome 类型（用户层/系统层/数据层）/未做正-负或目标-非目标双向对照。
- **聚类 B（2/6）矩阵形式发明**：要求 M×N 逐行命中-未命中表格（out 项与约束项进矩阵列）/「文字串描述而非结构化清单」。
- **聚类 C（1/6）跨步串号幻觉**：「三条 sub_step 1/2/3 混入违反单一记录」——v2.79 harness 义务句注在场仍发作（⑧ 已知跨 gate 通用幻觉，gate 侧「锚点非判对象」补位，u:2#2 同款）。
- **聚类 D（1/6）双字段误读**：「in[2] 基线快照并非改动实现指针」——发明「实现指针必须是待改文件」要件（只读核对项合法）。
- **聚类 E（1/6）约束回写明细发明**：要求「每条约束→回写位置/字段」级明细（prose 指出哪条约束怎么缩即合规）。

**vio2 基线牙齿保真度（㉖）**：5/6 命中但只 3/5 引对条款（放水），2/5 错理由（矩阵形式/out 项未进矩阵=clean 误伤同族）——设计内失牙风险牙，反转后牙必须由方框二接住（验证抽读确认：v6 落地轮 vio2 判词 3/6 逐字引方框二「零概念重叠/硬连」，余 3 颗为载荷自身的 forward 未点名形式牙，非设计承载牙）。

## 2. 迭代史（单变量：每轮只改 gate 文本层，v5 轮连带载荷修，归因族可分）

| 轮 | RESULT_JSON | 变更 | 教训 |
|---|---|---|---|
| v1 | clean 5/6, vio1-4 全 6/6, **vio5 2/6** | 初版四方框 | ⑫ 实例：默认-PASS 下未明列的违规形态（汇总声明）judge 不主动判——形式判据的违规形态也须成方框，移植 u:2#2 方框三 |
| v2 | clean 5/6, **vio2 2/6**, vio5 5/6 | +方框五 +方框四双侧 +回写措辞（「prose 一条即算」被反读成「必须逐条」——㉓第四变体：合法形态的量化词被反读为枚举要求） | vio2 判官方差带现形（同文本 v1 5/6→v2 2/6） |
| v3 | clean 5/6, vio2 4/6, vio5 6/6 | 方框二+「产出/核对关系」 | ⑬ 新变体：block 侧强化词（产出/核对关系）被 judge 升级成 elaboration 深度要求，反伤 clean（「数量来源=统计口径复算」被判裸断言） |
| v4 | clean 5/6, **vio2 0/6**, vio1 4/6 | 方框二逐字合法正例（「in[1]→G1（正 IC 数量来源=…）」） | **㉟ 新沉淀：方框内逐字合法正例会合法化同构违规词形**——vio2「in[3]→G1（CI 支撑门槛决策）」与 clean 正例同为「X→G1（…）」一句，判官按词形相似放行，牙齿全崩 |
| v5 | clean 5/6, vio2 4/6, vio1 6/6 | 方框二回 v3 前置例（CI 裸断言）+载荷修（forward 点名关系类别，⑦ 先查载荷缺陷第五实例：clean forward「G1→in[1]+in[2] 全覆盖」是方框五边界形态，诱发「标识也须给关系类别」发明要件——改生产真实形态「G1→in[1]（数量来源复算）+in[2]（规模基线核对）」） | clean [6] 判词自答「已逐项点名，非汇总泛指，应放行」仍 pass=False——**㉑ 自相矛盾判词=推理底**第二实证，停止钉死 |
| **v6** | **clean 6/6 + vio1-5 全 6/6** | 方框二收窄（删「X 支撑 Y 裸断言」冗余短语与逐字正例，留 CI 前置例+自称承接不免判+承接强弱/机制展开不审查） | 方框措辞信噪比：每个从句都要么是判官需要的识别锚（CI 前置例），要么是 clean 保护钉（强弱不审查），冗余识别词（「裸断言」第二表述）稀释信号 |
| 落地轮×3（同 v6 文本） | clean 6/6/5，vio1 全 6，**vio2 3/4/3**，vio3 6/5/6，vio4 全 6，vio5 6/4/5 | ①载荷修（㊱承载牙纪律：vio2 的 in[3] 从段尾悬挂改完整编入清单+矩阵，唯一违规=放水本身）；②无 gate 变更 | **推理底收盘判定**：vio2 九轮同/异文本 0-6/6 方差=概念重叠语义判据的判官能力边缘（文本变体已穷尽）；clean 两例 miss 均为判词逐字引用合法判例原文后仍判违规（公然违例=㉑ 自相矛盾同族，停止钉死）。读数口径入 replay docstring：vio2 牙齿按判词引方框二计数不按命中率 |

**与基线的诚实对照**：从严基线 vio2 5/6（仅 3/5 引对条款，余为超 vigilant 错理由牙——靠误伤 clean 的同族判词拦对）；反转后 vio2 带 3-6/6 但判词引对率升（落地轮 2/3-3/4 逐字引方框二）。牙齿强度小幅让渡换 clean 0/6→6/6，即 #28 反转的既定交易；vio2 残余漏判率=该 judge 对概念重叠判据的固有能力边界，非 framing 回潮。

三向验证（⑥）：clean 6/6 ✓；vio 全 6/6 ✓（牙齿保真度抽读：vio5 6/6 逐字引方框五，vio2 3/6 逐字引方框二+3 颗载荷自带形式牙，vio1/vio3/vio4 引对方框）✓；pin 测试全绿见 §4。

## 3. 终版 gate（v6，1341 字）

见 `dl_flow_nodes.py` understand:3 sub_steps[2].gate。结构：evidence 存在句+形式要件（含 prose/不要求表格钉死）→「默认 pass——仅当以下成立才判 block」+ 五方框（out 空清单/矩阵放水/outcome 空泛/替用户拍板/汇总无矩阵，每方框 block 条件+合法形态近端共存）→【合法正例】（只读核对项/prose 逐项/out 不进矩阵行/孤儿一句/前序锚点非判对象）→「方框以外一律不判」+ reason 引条款附范例尾句。

pin 保留：「矩阵放水」「拍板」「双字段」「sub_step==3」均在场（`test_step3_scope_two_sided`/`test_sub3_gate_checks_dual_field` 不动）；新增 `test_default_pass_marker_pinned_in_gates` u:3#3 行（framing 单源标记 pin）。

## 4. 落地清单

- [x] tests/replays/replay_u3_sub3.py（载荷内嵌，artifact=子1+子2+子3 三行生产形态）
- [x] tests/replays/README.md 清单行
- [x] dl_flow_nodes.py u:3#3 gate 替换（Python 脚本+.txt 载体+strip 一致性断言，㉕——首轮丢 8 个 \n 被断言当场逮住，显式 \n 转义重落地，1341 字/8 换行）
- [x] tests/test_dl_flow_engine.py `test_default_pass_marker_pinned_in_gates` 加 u:3#3 行
- [x] pytest 全绿（728）+ ruff（仅本批文件）
- [x] skill 沉淀（#30 泛化第十二例：㉟逐字合法正例合法化同构违规词形/㊱承载牙纪律（㉖载荷侧对偶）/㊲推理底扩展=公然违例+方差带读数口径）

## 5. 在飞 wf 影响评估（#30 ①）

demo（understand 子5 停置，2026-07-27 后无活动）与 tail_volume（子2 停置，state-reset 余档）均不走 u:3#3 当前步；gate 文本即改即生效只影响未来走到该步的实例，无误伤面。
