# dl-workflow .gitignore
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# 运行态：hook 日志 + workflow state.json 等（.claude/ 由 dl_flow_engine hook 写入，不入库）
.claude/

# codegraph 索引（机器本地，不入库）
.codegraph/

# 重放脚本 token（tests/replays/_common.py 读取，禁入库）
tests/replays/.token
